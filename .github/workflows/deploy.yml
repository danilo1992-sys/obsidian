name: Trigger Netlify Build

on:
  push:
    branches:
      - master # Cambia a 'main' si esa es la rama principal de tus notas

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Notificar a Netlify
        run: |
          curl -X POST -d '' ${{ secrets.NETLIFY_OBSIDIAN }}
